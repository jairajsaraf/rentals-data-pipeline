name: "rental_market"
version: "1.0.0"
config-version: 2

profile: "rental_market"

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
  - "logs"

models:
  rental_market:
    staging:
      +materialized: view
    mart:
      +materialized: table
